add the rest of the relational tests
	>
	>=
	<
	<=

add tests for var::less_var()

add lists

add arrays

add sets

add dicts

add mathematical operators
	+
	-
	*
	/
	+=
	-=
	*=
	/=
	
add boolean operators
	!
	&&
	||
	&&=
	||=
	
add bitwise operators
	~
	&
	|
	&=
	|=
	
add serialization

add algorithms
	strings
		comparison - [i]eq/[i]ne/[i]lt/[i]le/[i]gt/[i]ge/[i]lex_cmp/all
		substr
		[i]contains
		[i]find/[i]find_first/[i]find_last/[i]find_nth/[i]find_head/[i]find_tail/[i]find_token/[i]find_all
		[i]find_format/[i]find_format_all
		[i]replace/[i]replace_first/[i]replace_last/[i]replace_nth/[i]replace_all/replace_head/replace_tail
		split
		tolower
		toupper
		capitalize
		trim/trim_if/trim_left/trim_left_if/trim_right/trim_right_if
		format
		[i]erase/[i]erase_first/[i]erase_last/[i]erase_nth
		[i]starts_with
		[i]ends_with
		[i]erase_all/[i]erase_head/[i]erase_tail
		join/join_if
		find_regex/replace_regex/erase_regex/find_all_regex/split_regex/join_if
	collections
		predicates
			contains
			is_subset_of
			is_superset_of
		replace/replace_if
		remove/remove_if
		append
		prepend
		pop
		drop (pop from front)
		copy/copy_if
		find/find_first_of/find_last_of/find_if
		count/count_if
		countall/countall_if
		foreach
		forall
		rforeach
		rforall
		map
		reduce
		sort/sort_by
		rsort/rsort_by
		merge/merge_by
		min
		max
		random_shuffle
		reverse
		rotate
		search
		unique